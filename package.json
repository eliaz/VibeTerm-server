{
  "name": "vibeterm-server",
  "version": "0.1.0",
  "type": "module",
  "private": false,
  "scripts": {
    "start": "./start-server.sh",
    "check": "node --check scripts/vibeterm-config-server.mjs"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0"
  },
  "engines": {
    "node": ">=20"
  }
}
